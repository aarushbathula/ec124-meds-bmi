# Generated Outputs

This directory stores figures and tables created by the replication scripts.

Typical outputs include:

- `output/figures/*.pdf`
- `output/tables/*.tex`

These generated files are excluded from Git by default and can be rebuilt locally from the code and the required input data.
